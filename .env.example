# ===========================================
# Barae Environment Configuration
# ===========================================
# Copy to .env and fill in required values
# Required fields are marked with (required)
#
# Usage:
#   Development: docker compose -f compose.dev.yml up
#   Testing:     docker compose -f compose.test.yml up --build
#   Production:  docker compose -f compose.prod.yml up -d
#
# Dev: accessible at http://localhost:8100 (Caddy proxies frontend + backend)
# Prod: Caddy auto-HTTPS on your domain (ports 80/443)

# -------------------------------------------
# Database (required)
# -------------------------------------------
POSTGRES_USER=barae
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=barae
# POSTGRES_HOST is set by docker compose (points to db service)
# POSTGRES_PORT is set by docker compose (5432)

# -------------------------------------------
# Backend Server
# -------------------------------------------
SERVER_HOST=0.0.0.0
SERVER_PORT=3000

# -------------------------------------------
# Security (required)
# -------------------------------------------
# Generate with: openssl rand -hex 32
APP_SECRET=change-this-to-a-random-string-at-least-32-chars

# Auth base URL (public-facing URL including /api/v1 path)
# Dev: http://localhost:8100/api/v1 (set by compose)
# Prod: https://yourdomain.com/api/v1
AUTH_BASE_URL=http://localhost:8100/api/v1

# -------------------------------------------
# Domain (production only)
# -------------------------------------------
# Set to your production domain for Caddy auto-HTTPS
# DOMAIN=yourdomain.com

# -------------------------------------------
# Email - SMTP
# -------------------------------------------
# Development: Uses Mailpit automatically (view emails at http://localhost:8025)
# These defaults are overridden by compose.dev.yml to use Mailpit
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_SECURE=false
EMAIL_FROM=Barae <noreply@barae.local>

# Production: Configure your SMTP provider
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-user
# SMTP_PASSWORD=your-smtp-password
# SMTP_SECURE=false
# EMAIL_FROM=Barae <noreply@yourdomain.com>
