# ===========================================
# Barae Environment Configuration
# ===========================================
# Copy to .env and fill in required values
# Required fields are marked with (required)
#
# Usage:
#   Development: docker compose -f compose.dev.yml up
#   Production:  docker compose -f compose.prod.yml up -d

# -------------------------------------------
# Database (required)
# -------------------------------------------
POSTGRES_USER=barae
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=barae
POSTGRES_PORT=5432

# -------------------------------------------
# Backend Server
# -------------------------------------------
SERVER_HOST=0.0.0.0
SERVER_PORT=3000

# -------------------------------------------
# Security (required)
# -------------------------------------------
# Generate with: openssl rand -hex 32
APP_SECRET=change-this-to-a-random-string-at-least-32-chars

# -------------------------------------------
# GitHub OAuth (optional)
# -------------------------------------------
# Create OAuth App at: https://github.com/settings/developers
# Authorization callback URL: http://localhost:3000/api/v1/auth/callback/github
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# -------------------------------------------
# Frontend
# -------------------------------------------
# Development: separate Vite server
VITE_API_URL=http://localhost:3000

# Production: your domain
FRONTEND_URL=http://localhost:5173

# Production HTTP port (nginx)
HTTP_PORT=80

# -------------------------------------------
# Email - Resend (optional)
# -------------------------------------------
# Without this, emails are logged to console
# Create API key at: https://resend.com/api-keys
RESEND_API_KEY=
EMAIL_FROM=Barae <noreply@yourdomain.com>
