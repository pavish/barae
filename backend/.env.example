# Server
SERVER_HOST=0.0.0.0
SERVER_PORT=3000

# Database
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=barae
POSTGRES_USER=postgres
POSTGRES_PASSWORD=

# App Secret (required)
APP_SECRET=change-this-in-production

# Auth - GitHub OAuth (optional, GitHub login disabled if not set)
# Create OAuth App at: https://github.com/settings/developers
# Callback URL: http://localhost:3000/api/auth/callback/github
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Frontend URL (for CORS and trusted origins)
FRONTEND_URL=http://localhost:5173

# Email - Resend (optional, emails logged to console if not set)
# Create API key at: https://resend.com/api-keys
RESEND_API_KEY=re_xxxxx
# Use verified domain email, or Resend's onboarding domain for testing
EMAIL_FROM=Barae <noreply@yourdomain.com>
