# ===========================================
# Backend Environment Configuration
# ===========================================
# For running backend outside Docker
# Copy to .env and fill in required values

# -------------------------------------------
# Server
# -------------------------------------------
SERVER_HOST=0.0.0.0
SERVER_PORT=3000

# -------------------------------------------
# Database (required)
# -------------------------------------------
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=barae
POSTGRES_USER=barae
POSTGRES_PASSWORD=

# -------------------------------------------
# Security (required)
# -------------------------------------------
# Generate with: openssl rand -hex 32
APP_SECRET=

# -------------------------------------------
# GitHub OAuth (optional)
# -------------------------------------------
# Create OAuth App at: https://github.com/settings/developers
# Authorization callback URL: http://localhost:3000/api/auth/callback/github
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# -------------------------------------------
# Frontend URL (for CORS)
# -------------------------------------------
FRONTEND_URL=http://localhost:5173

# -------------------------------------------
# Email - SMTP (optional)
# -------------------------------------------
# Without SMTP_HOST, emails are logged to console
# For local dev, use Mailpit (included in compose.dev.yml):
#   SMTP_HOST=localhost (or mailpit in Docker)
#   SMTP_PORT=1025
#   SMTP_SECURE=false
# For production, use your SMTP provider
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_SECURE=false
EMAIL_FROM=Barae <noreply@yourdomain.com>
